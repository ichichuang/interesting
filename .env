# * 使用建议

# * 仅在 公共 .env 写所有环境都会用到的变量；

# * 若开发 / 生产需覆盖或新增其它变量，分别放入 .env.development、.env.production 并同样写上注释；

# * 布尔 / 数值在 Vite 注入时仍是字符串，可在代码里 import.meta.env.VITE_DEBUG === 'true' 手动转型。


# ==========================================
# 应用基础配置
# ==========================================



# 开发服务器端口
VITE_PORT=8888

# 是否启用 debug 模式（true | false）
VITE_DEBUG=false

# 路由模式
VITE_ROUTER_MODE=hash



# ==========================================
# 构建优化配置
# ==========================================

# 是否启用构建分析（true | false）- 生成打包体积报告
VITE_BUILD_ANALYZE=false

# 是否生成 source map 文件（true | false）
VITE_BUILD_SOURCEMAP=true

# 构建产物压缩方式（none | gzip | brotli | both）
VITE_COMPRESSION=none

# 是否启用 legacy 模式以支持旧版浏览器（true | false）
VITE_LEGACY=false

# 是否使用 CDN 加载第三方依赖（true | false）
VITE_CDN=false
